/**
 * 
 */
package j.jave.framework.commons.io.memory;

/**
 * all sub-interface can hold some dynamic resource in memory cache need extends the interface. 
 * that means all interfaces extends this have the ability of hold dynamic resource in memory , otherwise the reverse .
 * is's only the mark to the similar functions.
 * @author J
 */
public interface JDynamicMemoryCacheIO {

}
