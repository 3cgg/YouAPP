/**
 * 
 */
package me.bunny.kernel._c.io.memory;

/**
 * all sub-interface can hold some static resource in memory cache need extends the interface. 
 * that means all interfaces extends this have the ability of hold static resource in memory , otherwise the reverse .
 * is's only the mark to the similar functions.
 * @author J
 */
public interface JStaticMemoryCacheIO {

}
